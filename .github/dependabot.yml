version: 2
updates:
  # Keep GitHub Actions up to date. Because the workflows pin actions to commit
  # SHAs, Dependabot bumps both the SHA and the trailing version comment.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
